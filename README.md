# VIMO Frontend

## Structure

- `vimo_frontend-u`: user Expo app
- `vimo_frontend-m`: administrator Expo app (pending migration)
- `shared`: shared entry and login UI

Run app commands from the repository root. Legacy split app folders are preserved as `vimo_frontend-u` and `vimo_frontend-m`.

